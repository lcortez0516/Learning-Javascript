//For Each Method - when we call the foreach helper, we pass in anonymou callback funcction
//this function gets called on time for every element in the array
//Whatever is inside the function, that logic happens

const colors = ["teal", "blue", "red", "green"];

//function
colors.forEach((color) => console.log(color));
